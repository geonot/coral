binary_value is b101
hex_value is 0xFF

flag is true
age is 25
status is flag ? 'active' ! 'inactive'

primes is (2, 3, 5, 7, 11)
empty_list is ()

config is (host: 'localhost', port: 8080)

fn test_unless(condition)
    unless condition
        x is 42

fn test_until()
    counter is 0
    until counter.gt(5)
        counter is counter + 1

fn test_iterate(items)
    iterate items
        current is $
